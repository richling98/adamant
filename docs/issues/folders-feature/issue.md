# Feature: Sidebar folders

Users need a one-level folder system in the sidebar so they can organize meetings into named groups: user-created folders at the top, unfiled meetings below, drag-and-drop into folders, inline rename, delete (meetings move to root), and a per-folder control to start a new meeting assigned to that folder.

This issue captures the product requirement; the implementation plan lives in `plan.md`.
